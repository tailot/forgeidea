---
input:
  schema:
    count: integer
    context: string
    language: string
  default:
    language: english
---
Generate {{count}} categories for classifying ideas related to: {{context}} (general brainstorming or project concepts).

Output a comma-separated string of category names only (e.g., Category One, Category Two, Category Three). No numbering, introductions, or explanations.

Language: {{language}}